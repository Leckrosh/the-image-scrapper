# Runtime dependencies for imagegrab v1 (Python 3.12).
# After installing, fetch the browser used for harvesting:
#     playwright install chromium
playwright>=1.40
httpx>=0.27
selectolax>=0.3
SQLAlchemy>=2.0
pillow>=10.0
imagehash>=4.3
